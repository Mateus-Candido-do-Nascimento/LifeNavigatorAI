from dotenv import load_dotenv
import os

load_dotenv()

SECRET_KEY = os.getenv('SECRET_KEY')
DEBUG = os.getenv('DEBUG', 'False') == 'True'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('DB_NAME'),
        'USER': os.getenv('DB_USER'),
        'PASSWORD': os.getenv('DB_PASSWORD'),
        'HOST': os.getenv('DB_HOST', 'localhost'),
        'PORT': os.getenv('DB_PORT', '5432'),
    }
}
SECRET_KEY = os.getenv('SECRET_KEY')

2. Adicionar os apps ao INSTALLED_APPS:
pythonINSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Adiciona esses:
    'rest_framework',
    'chat',
    'data',
]


# fazendo as alterações fazer Faz essas duas alterações, salva e roda:

powershellpython manage.py migrate

# C:\Users\mattu\LifeNavigatorAI\backend> python manage.py migrate
Operations to perform:
  Apply all migrations: admin, auth, contenttypes, sessions
Running migrations:
  Applying contenttypes.0001_initial... OK
  Applying auth.0001_initial... OK
  Applying admin.0001_initial... OK
  Applying admin.0002_logentry_remove_auto_add... OK
  Applying admin.0003_logentry_add_action_flag_choices... OK
  Applying contenttypes.0002_remove_content_type_name... OK
  Applying auth.0002_alter_permission_name_max_length... OK
  Applying auth.0003_alter_user_email_max_length... OK
  Applying auth.0004_alter_user_username_opts... OK
  Applying auth.0005_alter_user_last_login_null... OK
  Applying auth.0006_require_contenttypes_0002... OK
  Applying auth.0007_alter_validators_add_error_messages... OK
  Applying auth.0008_alter_user_username_max_length... OK
  Applying auth.0009_alter_user_last_name_max_length... OK
  Applying auth.0010_alter_group_name_max_length... OK
  Applying auth.0011_update_proxy_permissions... OK
  Applying auth.0012_alter_user_first_name_max_length... OK
  Applying sessions.0001_initial... OK


O Django criou todas as tabelas internas dele no PostgreSQL. Vamos confirmar o que foi criado:

contenttypes → sistema interno do Django para rastrear os models

auth (0001 ao 0012) → tabelas de usuários, grupos e permissões

admin → tabelas do painel administrativo
sessions → tabelas de sessões de usuário

# como ver isso no pgadmin?

Sem problema! Vou te guiar. O pgAdmin tem uma navegação em árvore no lado esquerdo. Você precisa expandir até chegar nas tabelas:
Clica nas setinhas nessa ordem:
PostgreSQL 18
  └── Databases        ← clica pra expandir
        └── lifenavigator   ← clica pra expandir
              └── Schemas
                    └── public
                          └── Tables   ← aqui estão as tabelas