# O groq_service.py é o mais simples de todos — só chama o Groq com o prompt montado e retorna a resposta

Simples porque toda a inteligência já está no prompt — o groq_service só faz a chamada.

client = Groq(api_key=settings.GROQ_API_KEY)
#  │      │         │
#  │      │         └── pega a chave do settings.py
#  │      │              que leu do .env
#  │      │
#  │      └── Groq() é uma classe do SDK
#  │           cria um objeto configurado
#  │           pra se comunicar com a API
#  │
#  └── guarda o objeto numa variável
#       pra usar em qualquer função do arquivo